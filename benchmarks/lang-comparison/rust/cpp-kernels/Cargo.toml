[package]
name        = "sapient-cpp-kernels"
description = "C++ transliterations of SAPIENT's hot CPU kernels — language-comparison study only, not shipped"
version     = "0.6.0"
edition     = "2021"
license     = "AGPL-3.0-only"
publish     = false

[dependencies]

[build-dependencies]
cc = "1.0"

[dev-dependencies]
